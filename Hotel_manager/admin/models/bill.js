class bill {
  constructor(id, identity, totalprice, isdelete, datetime) {
    this.id = id;
    this.identity = identity; //CMND or CCCD
    this.totalprice = totalprice; //Tổng tiền
    this.isdelete = isdelete;
    this.datetime = datetime; //Ngày lập
  }
}

